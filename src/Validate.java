public class Validate {
    /**
     * Input validation functions (regex operations?)
     * - Ports
     * - IPs
     * - String cleaning
     * - Valid Keys format (2066)
     */

    public boolean isValidIp(String args) {
        boolean isValid = false;
        String splitString;
        int[] parsedInts;
        int[] moreThan4Items;
        int[] max255;

        return isValid;
    }

    public boolean isValidPort(String args) {
        boolean isValid = false;

        return isValid;
    }

    public boolean isCleanString(String args) {
        boolean isValid = false;

        return isValid;
    }

    public boolean isValidKeyFormat(String args) {
        boolean isValid = false;

        return isValid;
    }
}
